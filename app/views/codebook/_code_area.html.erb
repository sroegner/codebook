        <tr class="<%= cycle('odd','even') %>"><td><%=h code_area.name %></td>
        <td><%- if code_area.code_document_ids.size == 0 -%>
              <%= link_to(l(:codebook_index_label_delete), :action => "destroy_category", :area_id => code_area.id)%>
            <%- end -%>
        </td></tr>
